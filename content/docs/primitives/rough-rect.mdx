---
title: RoughRect
description: Hand-drawn rectangle primitive for borders and containers.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { RoughRect } from "@/components/primitives/rough-rect";

<PreviewContainer
  componentName="rough-rect"
  code={`import { RoughRect } from '@/components/crumble/primitives/rough-rect'

<RoughRect padding={20}>Primitive container</RoughRect>`}
>
  <RoughRect padding={20}>Primitive container</RoughRect>
</PreviewContainer>

## Installation

Primitives are bundled with any Crumble component install. To use standalone, copy `components/primitives/rough-rect.tsx` into your project.

## Props

<PropsTable rows={[
  { prop: 'animateOnHover', type: "boolean", default: undefined, description: "Redraws on hover." },
  { prop: 'children', type: "ReactNode", default: undefined, description: "Content rendered inside the component." },
  { prop: 'className', type: "string", default: undefined, description: "Additional Tailwind classes applied to the component." },
  { prop: 'fill', type: "string", default: undefined, description: "Overrides the fill color." },
  { prop: 'id', type: "string", default: undefined, description: "Controls the `id` behavior." },
  { prop: 'onClick', type: "() => void", default: undefined, description: "Controls the `onClick` behavior." },
  { prop: 'padding', type: "number", default: undefined, description: "Adds padding around the content." },
  { prop: 'rounded', type: "boolean", default: undefined, description: "Controls the `rounded` behavior." },
  { prop: 'stroke', type: "string", default: undefined, description: "Overrides the stroke color." },
  { prop: 'style', type: "CSSProperties", default: undefined, description: "Inline styles applied to the wrapper." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
]} />

## Accessibility

This primitive is primarily presentational. Keep the underlying content semantic and avoid relying on the sketch overlay alone to communicate meaning.

## Examples

```tsx
import { RoughRect } from '@/components/crumble/primitives/rough-rect'

<RoughRect padding={20}>Primitive container</RoughRect>
```
