---
title: Card
description: A hand-drawn card container with an optional stacked paper effect.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { Button } from "@/registry/new-york/ui/button";
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from "@/registry/new-york/ui/card";

<PreviewContainer
  componentName="card"
  code={`import { Card } from '@/components/crumble/ui/card'

<Card padding={20}>
  <p className="font-medium">Card title</p>
  <p className="mt-1 text-sm text-muted-foreground">Some content here.</p>
</Card>`}
>
  <Card padding={20}>
   
    <p className="text-sm font-medium">Card title</p>
    <p className="mt-1 text-xs text-muted-foreground">Some content goes here.</p>
  </Card>
</PreviewContainer>

## Installation

<InstallTabs slug="card" peerDeps={["roughjs"]} />

## Props

<PropsTable
  rows={[
    {
      prop: "children",
      type: "ReactNode",
      default: undefined,
      description: "Content rendered inside the component.",
      required: true,
    },
    {
      prop: "id",
      type: "string",
      default: undefined,
      description:
        "Optional stable id used for sketch seeding and accessibility.",
    },
    {
      prop: "padding",
      type: "number",
      default: "20",
      description: "Adds padding around the component content.",
    },
    {
      prop: "stacked",
      type: "boolean",
      default: "false",
      description: "Controls the `stacked` behavior.",
    },
    {
      prop: "style",
      type: "CSSProperties",
      default: undefined,
      description: "Controls the `style` behavior.",
    },
    {
      prop: "theme",
      type: "CrumbleTheme",
      default: undefined,
      description: "Overrides the global Crumble theme for this instance.",
    },
  ]}
/>

## Stacked effect

<PreviewContainer
  componentName="card"
  code={`<Card stacked padding={20}>
  <p className="font-medium">Stacked card</p>
  <p className="mt-1 text-sm text-muted-foreground">Feels like a stack of paper.</p>
</Card>`}
>
  <Card stacked padding={20}>
    <p className="text-sm font-medium">Stacked card</p>
    <p className="mt-1 text-xs text-muted-foreground">
      Feels like a stack of paper.
    </p>
  </Card>
</PreviewContainer>

## With sub-components

<PreviewContainer
  componentName="card"
  code={`<Card padding={24}>
  <CardHeader>
    <CardTitle>Getting started</CardTitle>
    <CardDescription>Install your first component.</CardDescription>
  </CardHeader>
  <CardContent>
    <p className="text-sm">Content goes here.</p>
  </CardContent>
  <CardFooter>
    <Button size="sm">Continue</Button>
  </CardFooter>
</Card>`}
>
  <Card padding={24}>
    <CardHeader>
      <CardTitle>Getting started</CardTitle>
      <CardDescription>Install your first component.</CardDescription>
    </CardHeader>
    <CardContent>
      <p className="text-sm text-muted-foreground">Content goes here.</p>
    </CardContent>
    <CardFooter>
      <Button size="sm">Continue</Button>
    </CardFooter>
  </Card>
</PreviewContainer>

## Custom stroke and fill

<PreviewContainer
  componentName="card"
  code={`<Card padding={20} stroke="oklch(0.6 0.18 260)" fill="oklch(0.96 0.04 240)">
  <p className="text-sm font-medium">Blue tinted card</p>
</Card>`}
>
  <Card padding={20} stroke="oklch(0.6 0.18 260)" fill="oklch(0.96 0.04 240)">
    <p className="text-sm font-medium">Blue tinted card</p>
  </Card>
</PreviewContainer>

## With Image

<PreviewContainer
  componentName="card"
  code={`<Card stacked padding={16}>
          <div className="relative"> 
            <div className="rotate-[-1.5deg] bg-white p-2 shadow-sm">
                <img src="https://images.unsplash.com/photo-1522202176988-66273c2fd55f" alt="Polaroid" className="h-40 w-full object-cover" />
            </div>
           <p className="mt-3 text-xs text-muted-foreground">
              Team moments, 2025
           </p>
          </div>
    </Card>`}
>
  <Card stacked padding={16}>
    {" "}
    <div className="relative">
      {" "}
      <div className="rotate-[-1.5deg] bg-white p-2 shadow-sm">
        {" "}
        <img
          src="https://cdn.cosmos.so/a53591ef-78c0-4f0e-8ba8-45cd5becd052?format=jpeg"
          alt="Polaroid"
          className="h-40 w-full object-cover"
        />{" "}
      </div>
      <p className="mt-3 text-xs text-muted-foreground">moments, 2025</p>
    </div>
  </Card>
</PreviewContainer>
## Accessibility

This component preserves the semantics of the underlying native elements where applicable. Provide visible labels and nearby text when the sketch styling is only decorative reinforcement.

## Examples

```tsx
import { Card } from "@/components/crumble/ui/card";

<Card padding={20}>
  <p className="font-medium">Card title</p>
  <p className="mt-1 text-sm text-muted-foreground">Some content here.</p>
</Card>;
```
