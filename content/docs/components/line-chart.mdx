---
title: Line Chart
description: A hand-drawn line chart with Catmull-Rom curves and animated draw-on.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer"
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { LineChart } from "@/registry/new-york/ui/line-chart"

<PreviewContainer
  componentName="line-chart"
  code={`import { LineChart } from '@/components/crumble/ui/line-chart'

<LineChart
  labels={["Jan", "Feb", "Mar", "Apr", "May", "Jun"]}
  series={[{ id: "revenue", label: "Revenue", data: [40, 65, 55, 80, 70, 90] }]}
/>`}
>
  <div className="w-full">
    <LineChart
      labels={["Jan", "Feb", "Mar", "Apr", "May", "Jun"]}
      series={[{ id: "revenue", label: "Revenue", data: [40, 65, 55, 80, 70, 90] }]}
    />
  </div>
</PreviewContainer>

## Installation

<InstallTabs slug="line-chart" peerDeps={["d3-shape", "d3-scale"]} />

## Props

<PropsTable rows={[
  { prop: 'animateOnMount', type: "boolean", default: "true", description: "Animates the component when it first renders." },
  { prop: 'formatValue', type: "(v: number) => string", default: undefined, description: "Formats the displayed numeric value." },
  { prop: 'formatX', type: "(i: number) => string", default: undefined, description: "Formats X-axis labels." },
  { prop: 'height', type: "number", default: "240", description: "Sets the rendered height in pixels." },
  { prop: 'id', type: "string", default: undefined, description: "Optional stable id used for sketch seeding and accessibility." },
  { prop: 'labels', type: "string[]", default: undefined, description: "Labels used by the component." },
  { prop: 'series', type: "LineChartSeries[]", default: undefined, description: "Series data used to render the component.", required: true },
  { prop: 'showDots', type: "boolean", default: "true", description: "Shows point markers for each chart value." },
  { prop: 'showGrid', type: "boolean", default: "true", description: "Shows background grid lines." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
]} />

## Multiple series

<PreviewContainer
  componentName="line-chart"
  code={`<LineChart
  labels={["Mon", "Tue", "Wed", "Thu", "Fri"]}
  series={[
    { id: "visits",    label: "Visits",    data: [120, 180, 150, 210, 190], color: "oklch(0.6 0.18 260)" },
    { id: "signups",   label: "Sign-ups",  data: [20,  35,  28,  42,  38],  color: "oklch(0.6 0.16 145)" },
  ]}
/>`}
>
  <div className="w-full">
    <LineChart
      labels={["Mon", "Tue", "Wed", "Thu", "Fri"]}
      series={[
        { id: "visits",  label: "Visits",   data: [120, 180, 150, 210, 190], color: "oklch(0.6 0.18 260)" },
        { id: "signups", label: "Sign-ups", data: [20,  35,  28,  42,  38],  color: "oklch(0.6 0.16 145)" },
      ]}
    />
  </div>
</PreviewContainer>

## Without dots

<PreviewContainer
  componentName="line-chart"
  code={`<LineChart
  showDots={false}
  labels={["W1","W2","W3","W4","W5","W6","W7","W8"]}
  series={[{ id: "s", label: "Score", data: [60, 55, 70, 65, 80, 75, 85, 90] }]}
/>`}
>
  <div className="w-full">
    <LineChart
      showDots={false}
      labels={["W1","W2","W3","W4","W5","W6","W7","W8"]}
      series={[{ id: "s", label: "Score", data: [60, 55, 70, 65, 80, 75, 85, 90] }]}
    />
  </div>
</PreviewContainer>

## Accessibility

LineChart is best treated as a visual aid rather than the only source of truth. Include a textual summary or supporting table if exact values and trends need to be accessible.

## Examples

```tsx
import { LineChart } from '@/components/crumble/ui/line-chart'

<LineChart
  labels={["Jan", "Feb", "Mar", "Apr", "May", "Jun"]}
  series={[{ id: "revenue", label: "Revenue", data: [40, 65, 55, 80, 70, 90] }]}
/>
```
