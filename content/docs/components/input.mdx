---
title: Input
description: A hand-drawn text input with box or underline style.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer"
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { Input } from "@/registry/new-york/ui/input"

<PreviewContainer
  componentName="input"
  code={`import { Input } from '@/components/crumble/ui/input'

<Input label="Name" placeholder="Enter your name" />`}
>
  <div className="w-full max-w-xs">
    <Input label="Name" placeholder="Enter your name" />
  </div>
</PreviewContainer>

## Installation

<InstallTabs slug="input" />

## Props

<PropsTable rows={[
  { prop: 'error', type: "string", default: undefined, description: "Error message shown below the component." },
  { prop: 'inputStyle', type: "InputStyle", default: "\"box\"", description: "Controls the `inputStyle` behavior." },
  { prop: 'label', type: "string", default: undefined, description: "Optional label text shown with the component." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
  { prop: 'value', type: "string", default: undefined, description: "Controlled input value." },
  { prop: 'defaultValue', type: "string", default: undefined, description: "Initial uncontrolled value." },
  { prop: 'placeholder', type: "string", default: undefined, description: "Placeholder text." },
  { prop: 'disabled', type: "boolean", default: undefined, description: "Disables the input." },
  { prop: 'onChange', type: "(event) => void", default: undefined, description: "Change handler." },
]} />

## Styles

<PreviewContainer
  componentName="input"
  code={`<Input label="Box style" inputStyle="box" placeholder="Box" />
<Input label="Underline style" inputStyle="underline" placeholder="Underline" />`}
>
  <div className="flex w-full max-w-xs flex-col gap-5">
    <Input label="Box style" inputStyle="box" placeholder="Box" />
    <Input label="Underline style" inputStyle="underline" placeholder="Underline" />
  </div>
</PreviewContainer>

## Error state

<PreviewContainer
  componentName="input"
  code={`<Input label="Email" placeholder="you@example.com" error="Invalid email address" />`}
>
  <div className="w-full max-w-xs">
    <Input label="Email" placeholder="you@example.com" error="Invalid email address" />
  </div>
</PreviewContainer>

## Disabled

<PreviewContainer
  componentName="input"
  code={`<Input label="Locked field" placeholder="Read only" disabled />`}
>
  <div className="w-full max-w-xs">
    <Input label="Locked field" placeholder="Read only" disabled />
  </div>
</PreviewContainer>

## Themes

<PreviewContainer
  componentName="input"
  code={`<Input label="Pencil" placeholder="pencil" theme="pencil" />
<Input label="Ink"    placeholder="ink"    theme="ink" />
<Input label="Crayon" placeholder="crayon" theme="crayon" />`}
>
  <div className="flex w-full max-w-xs flex-col gap-5">
    <Input label="Pencil" placeholder="pencil" theme="pencil" />
    <Input label="Ink"    placeholder="ink"    theme="ink" />
    <Input label="Crayon" placeholder="crayon" theme="crayon" />
  </div>
</PreviewContainer>

## Accessibility

Input renders a native text input with a real associated `label` when provided. Error text stays in the document flow, so you can connect it to form-level validation messaging as needed.

## Examples

```tsx
import { Input } from '@/components/crumble/ui/input'

<Input label="Name" placeholder="Enter your name" />
```
