---
title: Color Picker
description: A hand-drawn color picker with rough-styled swatches and hex input.
---

---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { ColorPicker } from "@/registry/new-york/ui/color-picker";
import ColorPickerDemo from "@/components/demos/colorPickerDemo";

<PreviewContainer
componentName="color-picker"
code={`import { ColorPicker } from '@/components/crumble/ui/color-picker'

<ColorPicker
label="Stroke Color"
defaultValue="#ef4444"
onChange={(color) => console.log(color)}
/>`}

>

<ColorPickerDemo />
</PreviewContainer>

## Installation

<InstallTabs slug="color-picker" peerDeps={["roughjs"]} />

## Props

<PropsTable
  rows={[
    {
      prop: "label",
      type: "string",
      default: undefined,
      description: "Optional label displayed above the picker.",
    },
    {
      prop: "defaultValue",
      type: "string",
      default: '"#3b82f6"',
      description: "Initial selected color (uncontrolled mode).",
    },
    {
      prop: "value",
      type: "string",
      default: undefined,
      description: "Controlled value of the selected color.",
    },
    {
      prop: "onChange",
      type: "(color: string) => void",
      default: undefined,
      description: "Callback fired when a color is selected or typed.",
    },
    {
      prop: "swatches",
      type: "string[]",
      default: "preset palette",
      description: "Array of color swatches displayed in the grid.",
    },
    {
      prop: "className",
      type: "string",
      default: undefined,
      description: "Additional Tailwind classes applied to the component.",
    },
    {
      prop: "id",
      type: "string",
      default: '"color-picker"',
      description:
        "Optional stable id used for sketch seeding and accessibility.",
    },
    {
      prop: "theme",
      type: "CrumbleTheme",
      default: undefined,
      description: "Overrides the global Crumble theme for this instance.",
    },
    {
      prop: "fill",
      type: "string",
      default: undefined,
      description: "Custom fill color for rough elements.",
    },
    {
      prop: "stroke",
      type: "string",
      default: undefined,
      description: "Custom stroke color for rough elements.",
    },
    {
      prop: "strokeMuted",
      type: "string",
      default: undefined,
      description: "Muted stroke color (used for borders).",
    },
  ]}
/>

## Custom swatches

<PreviewContainer
componentName="color-picker"
code={`<ColorPicker
  swatches={['#000000', '#ffff42', '#ff0000']}
/>`}

>

<ColorPicker swatches={["#000000", "#ffff42", "#ff0000"]} /> </PreviewContainer>

## Custom stroke and fill

<PreviewContainer
componentName="color-picker"
code={`<ColorPicker
  stroke="oklch(0.6 0.18 260)"
  fill="oklch(0.96 0.04 240)"
/>`}

>

<ColorPicker
 stroke="oklch(0.6 0.18 260)"
 fill="oklch(0.96 0.04 240)"
/> </PreviewContainer>

## Accessibility

This component preserves semantic input behavior. Ensure labels are provided when used in forms and that color selection is not the only means of conveying meaning.

## Examples

```tsx
import { ColorPicker } from "@/components/crumble/ui/color-picker";

<ColorPicker
  label="Pick a color"
  defaultValue="#3b82f6"
  onChange={(color) => console.log(color)}
/>;
```
