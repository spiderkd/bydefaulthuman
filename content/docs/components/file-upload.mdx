---
title: File Upload
description: A hand-drawn dropzone that becomes more expressive when a file is dragged over it.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer"
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { FileUpload } from "@/registry/new-york/ui/file-upload"

<PreviewContainer
  componentName="file-upload"
  code={`import { FileUpload } from '@/components/crumble/ui/file-upload'

<FileUpload />`}
>
  <div className="w-full max-w-sm">
    <FileUpload />
  </div>
</PreviewContainer>

## Installation

<InstallTabs slug="file-upload" />

## Props

<PropsTable rows={[
  { prop: 'accept', type: "string", default: undefined, description: "Accepted file types." },
  { prop: 'className', type: "string", default: undefined, description: "Additional Tailwind classes applied to the component." },
  { prop: 'disabled', type: "boolean", default: "false", description: "Disables interaction." },
  { prop: 'id', type: "string", default: undefined, description: "Optional stable id used for sketch seeding and accessibility." },
  { prop: 'label', type: "string", default: undefined, description: "Optional label text shown with the component." },
  { prop: 'multiple', type: "boolean", default: "false", description: "Allows handling multiple values or files." },
  { prop: 'onChange', type: "(files: FileList) => void", default: undefined, description: "Callback fired when the value changes." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
]} />

## With hint and accept

<PreviewContainer
  componentName="file-upload"
  code={`<FileUpload
  label="Drop images here or click to browse"
  hint="PNG, JPG up to 5 MB"
  accept="image/*"
/>`}
>
  <div className="w-full max-w-sm">
    <FileUpload
      label="Drop images here or click to browse"
      hint="PNG, JPG up to 5 MB"
      accept="image/*"
    />
  </div>
</PreviewContainer>

## Multiple files

<PreviewContainer
  componentName="file-upload"
  code={`<FileUpload multiple hint="Select multiple files" />`}
>
  <div className="w-full max-w-sm">
    <FileUpload multiple hint="Select multiple files" />
  </div>
</PreviewContainer>

## Themes

<PreviewContainer
  componentName="file-upload"
  code={`<FileUpload theme="crayon" label="Drop here" />`}
>
  <div className="w-full max-w-sm">
    <FileUpload theme="crayon" label="Drop here" />
  </div>
</PreviewContainer>

## Accessibility

FileUpload still uses a native file input under the custom dropzone, so browser file picker behavior and keyboard activation remain available. Add surrounding copy that explains accepted file types and size limits when needed.

## Examples

```tsx
import { FileUpload } from '@/components/crumble/ui/file-upload'

<FileUpload />
```
