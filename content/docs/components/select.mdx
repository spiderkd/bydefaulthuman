---
title: Select
description: A hand-drawn select dropdown with a rough chevron indicator.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { Select } from "@/registry/new-york/ui/select";

<PreviewContainer
  componentName="select"
  code={`import { Select } from '@/components/crumble/ui/select'

<Select
  label="Framework"
  placeholder="Choose one..."
  options={[
    { value: 'next', label: 'Next.js' },
    { value: 'remix', label: 'Remix' },
    { value: 'astro', label: 'Astro' },
  ]}
/>`}
>
  <div className="w-full max-w-xs">
    <Select
      label="Framework"
      placeholder="Choose one..."
      options={[
        { value: "next", label: "Next.js" },
        { value: "remix", label: "Remix" },
        { value: "astro", label: "Astro" },
      ]}
    />
  </div>
</PreviewContainer>

## Installation

<InstallTabs slug="select" />

## Props

<PropsTable rows={[
  { prop: 'className', type: "string", default: undefined, description: "Additional Tailwind classes applied to the component." },
  { prop: 'defaultValue', type: "string", default: undefined, description: "Initial uncontrolled value." },
  { prop: 'disabled', type: "boolean", default: undefined, description: "Disables interaction." },
  { prop: 'error', type: "string", default: undefined, description: "Error message shown below the component." },
  { prop: 'id', type: "string", default: undefined, description: "Optional stable id used for sketch seeding and accessibility." },
  { prop: 'label', type: "string", default: undefined, description: "Optional label text shown with the component." },
  { prop: 'onBlur', type: "() => void", default: undefined, description: "Optional callback for component events." },
  { prop: 'onChange', type: "(value: string) => void", default: undefined, description: "Callback fired when the value changes." },
  { prop: 'onFocus', type: "() => void", default: undefined, description: "Optional callback for component events." },
  { prop: 'options', type: "SelectOption[]", default: undefined, description: "Options rendered by the component.", required: true },
  { prop: 'placeholder', type: "string", default: undefined, description: "Placeholder text shown before a value is entered or selected." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
  { prop: 'value', type: "string", default: undefined, description: "Controlled value for the component." },
]} />

## Error state

<PreviewContainer
  componentName="select"
  code={`<Select
  label="Country"
  placeholder="Select a country"
  error="Please select a country"
  options={[{ value: 'in', label: 'India' }, { value: 'us', label: 'United States' }]}
/>`}
>
  <div className="w-full max-w-xs">
    <Select
      label="Country"
      placeholder="Select a country"
      error="Please select a country"
      options={[
        { value: "in", label: "India" },
        { value: "us", label: "United States" },
      ]}
    />
  </div>
</PreviewContainer>

## Accessibility

Select uses a button plus listbox pattern, so the trigger should have a clear label or surrounding heading. Keep option labels distinct and short to make keyboard selection easier.

## Examples

```tsx
import { Select } from '@/components/crumble/ui/select'

<Select
  label="Framework"
  placeholder="Choose one..."
  options={[
    { value: 'next', label: 'Next.js' },
    { value: 'remix', label: 'Remix' },
    { value: 'astro', label: 'Astro' },
  ]}
/>
```
