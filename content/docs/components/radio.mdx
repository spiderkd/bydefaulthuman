---
title: Radio
description: Hand-drawn radio buttons with a filled inner circle when selected.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { RadioGroup } from "@/registry/new-york/ui/radio";

<PreviewContainer
  componentName="radio"
  code={`import { RadioGroup } from '@/components/crumble/ui/radio'

<RadioGroup
  name="plan"
  defaultValue="monthly"
  options={[
    { value: 'monthly',  label: 'Monthly' },
    { value: 'yearly',   label: 'Yearly' },
    { value: 'lifetime', label: 'Lifetime' },
  ]}
/>`}
>
  <RadioGroup
    name="plan-preview"
    defaultValue="monthly"
    options={[
      { value: "monthly",  label: "Monthly" },
      { value: "yearly",   label: "Yearly" },
      { value: "lifetime", label: "Lifetime" },
    ]}
  />
</PreviewContainer>

## Installation

<InstallTabs slug="radio" />

## Props

<PropsTable rows={[
  { prop: 'className', type: "string", default: undefined, description: "Additional Tailwind classes applied to the component." },
  { prop: 'defaultValue', type: "string", default: undefined, description: "Initial uncontrolled value." },
  { prop: 'name', type: "string", default: undefined, description: "Controls the `name` behavior.", required: true },
  { prop: 'onChange', type: "(value: string) => void", default: undefined, description: "Callback fired when the value changes." },
  { prop: 'options', type: "RadioOption[]", default: undefined, description: "Options rendered by the component.", required: true },
  { prop: 'orientation', type: "\"vertical\" | \"horizontal\"", default: "\"vertical\"", description: "Controls the component layout direction." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
  { prop: 'value', type: "string", default: undefined, description: "Controlled value for the component." },
]} />

## Horizontal layout

<PreviewContainer
  componentName="radio"
  code={`<RadioGroup
  name="size"
  orientation="horizontal"
  defaultValue="md"
  options={[
    { value: 'sm', label: 'S' },
    { value: 'md', label: 'M' },
    { value: 'lg', label: 'L' },
    { value: 'xl', label: 'XL' },
  ]}
/>`}
>
  <RadioGroup
    name="size-preview"
    orientation="horizontal"
    defaultValue="md"
    options={[
      { value: "sm", label: "S" },
      { value: "md", label: "M" },
      { value: "lg", label: "L" },
      { value: "xl", label: "XL" },
    ]}
  />
</PreviewContainer>

## With disabled option

<PreviewContainer
  componentName="radio"
  code={`<RadioGroup
  name="tier"
  defaultValue="free"
  options={[
    { value: 'free', label: 'Free' },
    { value: 'pro',  label: 'Pro' },
    { value: 'enterprise', label: 'Enterprise', disabled: true },
  ]}
/>`}
>
  <RadioGroup
    name="tier-preview"
    defaultValue="free"
    options={[
      { value: "free", label: "Free" },
      { value: "pro", label: "Pro" },
      { value: "enterprise", label: "Enterprise", disabled: true },
    ]}
  />
</PreviewContainer>

## Themes

<PreviewContainer
  componentName="radio"
  code={`<RadioGroup
  name="theme-demo"
  theme="crayon"
  defaultValue="b"
  options={[
    { value: 'a', label: 'Option A' },
    { value: 'b', label: 'Option B' },
  ]}
/>`}
>
  <RadioGroup
    name="theme-demo"
    theme="crayon"
    defaultValue="b"
    options={[
      { value: "a", label: "Option A" },
      { value: "b", label: "Option B" },
    ]}
  />
</PreviewContainer>

## Accessibility

RadioGroup renders native radio inputs, so arrow-key navigation and form behavior follow browser conventions. Use a shared `name` and a visible group label when the choice set needs extra context.

## Examples

```tsx
import { RadioGroup } from '@/components/crumble/ui/radio'

<RadioGroup
  name="plan"
  defaultValue="monthly"
  options={[
    { value: 'monthly',  label: 'Monthly' },
    { value: 'yearly',   label: 'Yearly' },
    { value: 'lifetime', label: 'Lifetime' },
  ]}
/>
```
