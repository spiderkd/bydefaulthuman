---
title: Textarea
description: A hand-drawn textarea with optional auto-grow and resize support.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { Textarea } from "@/registry/new-york/ui/textarea";

<PreviewContainer
  componentName="textarea"
  code={`import { Textarea } from '@/components/crumble/ui/textarea'

<Textarea label="Message" placeholder="Write something..." />`}
>
  <div className="w-full max-w-xs">
    <Textarea label="Message" placeholder="Write something..." />
  </div>
</PreviewContainer>

## Installation

<InstallTabs slug="textarea" />

## Props

<PropsTable rows={[
  { prop: 'autoGrow', type: "boolean", default: "false", description: "Controls the `autoGrow` behavior." },
  { prop: 'error', type: "string", default: undefined, description: "Error message shown below the component." },
  { prop: 'label', type: "string", default: undefined, description: "Optional label text shown with the component." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
  { prop: 'value', type: "string", default: undefined, description: "Controlled textarea value." },
  { prop: 'defaultValue', type: "string", default: undefined, description: "Initial uncontrolled value." },
  { prop: 'placeholder', type: "string", default: undefined, description: "Placeholder text." },
  { prop: 'rows', type: "number", default: undefined, description: "Visible row count when auto-grow is off." },
  { prop: 'disabled', type: "boolean", default: undefined, description: "Disables the textarea." },
  { prop: 'onChange', type: "(event) => void", default: undefined, description: "Change handler." },
]} />

## Auto-grow

The textarea expands as you type. The SVG border tracks the height via `ResizeObserver`.

<PreviewContainer
  componentName="textarea"
  code={`<Textarea label="Notes" placeholder="Starts small, grows as you type..." autoGrow />`}
>
  <div className="w-full max-w-xs">
    <Textarea
      label="Notes"
      placeholder="Starts small, grows as you type..."
      autoGrow
    />
  </div>
</PreviewContainer>

## Error state

<PreviewContainer
  componentName="textarea"
  code={`<Textarea label="Message" error="Message is required" />`}
>
  <div className="w-full max-w-xs">
    <Textarea label="Message" error="Message is required" />
  </div>
</PreviewContainer>

## Disabled

<PreviewContainer
  componentName="textarea"
  code={`<Textarea label="Locked" placeholder="Cannot edit" disabled />`}
>
  <div className="w-full max-w-xs">
    <Textarea label="Locked" placeholder="Cannot edit" disabled />
  </div>
</PreviewContainer>

## Themes

<PreviewContainer
  componentName="textarea"
  code={`<Textarea label="Pencil" placeholder="pencil" theme="pencil" />
<Textarea label="Ink"    placeholder="ink"    theme="ink" />
<Textarea label="Crayon" placeholder="crayon" theme="crayon" />`}
>
  <div className="flex w-full max-w-xs flex-col gap-5">
    <Textarea label="Pencil" placeholder="pencil" theme="pencil" />
    <Textarea label="Ink" placeholder="ink" theme="ink" />
    <Textarea label="Crayon" placeholder="crayon" theme="crayon" />
  </div>
</PreviewContainer>

## Accessibility

Textarea renders a native `textarea` element and keeps the label in the DOM for assistive technologies. When using `autoGrow`, the resizing is visual only and does not change keyboard behavior.

## Examples

```tsx
import { Textarea } from '@/components/crumble/ui/textarea'

<Textarea label="Message" placeholder="Write something..." />
```
