---
title: Slider
description: A hand-drawn range slider with a sketchy track and circle thumb.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer"
import { Slider } from "@/components/ui/slider"

<PreviewContainer
  componentName="slider"
  code={`import { Slider } from '@/components/crumble/ui/slider'

<Slider label="Volume" defaultValue={40} />`}
>
  <div className="w-full max-w-xs">
    <Slider label="Volume" defaultValue={40} />
  </div>
</PreviewContainer>

## Installation

```bash
npx shadcn add https://crumble.dev/r/slider.json
```

## Custom range and format

<PreviewContainer
  componentName="slider"
  code={`<Slider
  label="Price"
  min={0}
  max={500}
  defaultValue={150}
  formatValue={(v) => \`$\${v}\`}
/>`}
>
  <div className="w-full max-w-xs">
    <Slider
      label="Price"
      min={0}
      max={500}
      defaultValue={150}
      formatValue={(v) => `$${v}`}
    />
  </div>
</PreviewContainer>

## Hide value

<PreviewContainer
  componentName="slider"
  code={`<Slider label="Opacity" defaultValue={70} showValue={false} />`}
>
  <div className="w-full max-w-xs">
    <Slider label="Opacity" defaultValue={70} showValue={false} />
  </div>
</PreviewContainer>

## Themes

<PreviewContainer
  componentName="slider"
  code={`<Slider label="Pencil" defaultValue={50} theme="pencil" />
<Slider label="Ink"    defaultValue={50} theme="ink" />
<Slider label="Crayon" defaultValue={50} theme="crayon" />`}
>
  <div className="flex w-full max-w-xs flex-col gap-5">
    <Slider label="Pencil" defaultValue={50} theme="pencil" />
    <Slider label="Ink"    defaultValue={50} theme="ink" />
    <Slider label="Crayon" defaultValue={50} theme="crayon" />
  </div>
</PreviewContainer>
