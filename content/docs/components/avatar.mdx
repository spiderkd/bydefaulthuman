---
title: Avatar
description: A hand-drawn circle border around a profile image or initials fallback.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer"
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { Avatar, AvatarGroup } from "@/registry/new-york/ui/avatar"

<PreviewContainer
  componentName="avatar"
  code={`import { Avatar } from '@/components/crumble/ui/avatar'

<Avatar fallback="SC" />`}
>
  <Avatar fallback="SC" />
</PreviewContainer>

## Installation

<InstallTabs slug="avatar" />

## Props

<PropsTable rows={[
  { prop: 'animateOnHover', type: "boolean", default: "true", description: "Redraws the sketch lines on hover." },
  { prop: 'className', type: "string", default: undefined, description: "Additional Tailwind classes applied to the component." },
  { prop: 'fallback', type: "string", default: undefined, description: "Fallback initials or text shown when no image is available." },
  { prop: 'id', type: "string", default: undefined, description: "Optional stable id used for sketch seeding and accessibility." },
  { prop: 'size', type: "number", default: "40", description: "Controls the visual size of the component." },
  { prop: 'src', type: "string", default: undefined, description: "Image source URL." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
]} />

## With image

<PreviewContainer
  componentName="avatar"
  code={`<Avatar src="https://github.com/shadcn.png" fallback="SC" />`}
>
  <Avatar src="https://github.com/shadcn.png" fallback="SC" />
</PreviewContainer>

## Sizes

<PreviewContainer
  componentName="avatar"
  code={`<Avatar fallback="SM" size={32} />
<Avatar fallback="MD" size={40} />
<Avatar fallback="LG" size={56} />
<Avatar fallback="XL" size={72} />`}
>
  <div className="flex items-end gap-3">
    <Avatar fallback="SM" size={32} />
    <Avatar fallback="MD" size={40} />
    <Avatar fallback="LG" size={56} />
    <Avatar fallback="XL" size={72} />
  </div>
</PreviewContainer>

## Stacked group

`AvatarGroup` takes an `avatars` array of props objects and a `max` count. Extra avatars show as a `+N` overflow badge.

<PreviewContainer
  componentName="avatar"
  code={`<AvatarGroup
  max={3}
  avatars={[
    { fallback: "AB" },
    { fallback: "CD" },
    { fallback: "EF" },
    { fallback: "GH" },
    { fallback: "IJ" },
  ]}
/>`}
>
  <AvatarGroup
    max={3}
    avatars={[
      { fallback: "AB" },
      { fallback: "CD" },
      { fallback: "EF" },
      { fallback: "GH" },
      { fallback: "IJ" },
    ]}
  />
</PreviewContainer>

## Themes

<PreviewContainer
  componentName="avatar"
  code={`<Avatar fallback="P" theme="pencil" />
<Avatar fallback="I" theme="ink" />
<Avatar fallback="C" theme="crayon" />`}
>
  <div className="flex gap-3">
    <Avatar fallback="P" theme="pencil" />
    <Avatar fallback="I" theme="ink" />
    <Avatar fallback="C" theme="crayon" />
  </div>
</PreviewContainer>

## Accessibility

Avatar content remains text-accessible because the fallback initials live in the DOM. If the avatar is interactive, wrap it in a semantic button or link instead of attaching click behavior to a generic div.

## Examples

```tsx
import { Avatar } from '@/components/crumble/ui/avatar'

<Avatar fallback="SC" />
```
