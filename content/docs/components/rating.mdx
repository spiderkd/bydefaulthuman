---
title: Rating
description: A hand-drawn star rating with hover fill and hachure.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { Rating } from "@/registry/new-york/ui/rating";

<PreviewContainer
  componentName="rating"
  code={`import { Rating } from '@/components/crumble/ui/rating'

<Rating defaultValue={3} />`}
>
  <Rating defaultValue={3} />
</PreviewContainer>

## Installation

<InstallTabs slug="rating" />

## Props

<PropsTable rows={[
  { prop: 'className', type: "string", default: undefined, description: "Additional Tailwind classes applied to the component." },
  { prop: 'defaultValue', type: "number", default: "0", description: "Initial uncontrolled value." },
  { prop: 'disabled', type: "boolean", default: undefined, description: "Disables interaction." },
  { prop: 'id', type: "string", default: undefined, description: "Optional stable id used for sketch seeding and accessibility." },
  { prop: 'label', type: "string", default: undefined, description: "Optional label text shown with the component." },
  { prop: 'max', type: "number", default: "5", description: "Upper bound for the value." },
  { prop: 'onChange', type: "(value: number) => void", default: undefined, description: "Callback fired when the value changes." },
  { prop: 'size', type: "number", default: "24", description: "Controls the visual size of the component." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
  { prop: 'value', type: "number", default: undefined, description: "Controlled value for the component." },
]} />

## Sizes

<PreviewContainer
  componentName="rating"
  code={`<Rating defaultValue={3} size={16} />
<Rating defaultValue={3} size={24} />
<Rating defaultValue={3} size={36} />`}
>
  <div className="flex flex-col gap-3">
    <Rating defaultValue={3} size={16} />
    <Rating defaultValue={3} size={24} />
    <Rating defaultValue={3} size={36} />
  </div>
</PreviewContainer>

## Read only

<PreviewContainer componentName="rating" code={`<Rating value={4} readOnly />`}>
  <Rating value={4} readOnly />
</PreviewContainer>

## Disabled

<PreviewContainer
  componentName="rating"
  code={`<Rating defaultValue={2} disabled />`}
>
  <Rating defaultValue={2} disabled />
</PreviewContainer>

## Themes

<PreviewContainer
  componentName="rating"
  code={`<Rating defaultValue={3} theme="pencil" />
<Rating defaultValue={3} theme="ink" />
<Rating defaultValue={3} theme="crayon" />`}
>
  <div className="flex flex-col gap-3">
    <Rating defaultValue={3} theme="pencil" />
    <Rating defaultValue={3} theme="ink" />
    <Rating defaultValue={3} theme="crayon" />
  </div>
</PreviewContainer>

## Accessibility

Rating uses button elements for each star, so keyboard focus still works. Pair the stars with a visible label or nearby text so the scale meaning is clear before a user selects a value.

## Examples

```tsx
import { Rating } from '@/components/crumble/ui/rating'

<Rating defaultValue={3} />
```
