---
title: Notebook
description: A hand-drawn lined paper container with ruled lines and a red margin.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { Notebook } from "@/registry/new-york/ui/notebook";

<PreviewContainer
  componentName="notebook"
  code={`import { Notebook } from '@/components/crumble/ui/notebook'

<Notebook>
  <p>Dear diary,</p>
  <p>Today I shipped the design system.</p>
</Notebook>`}
>
  <div className="w-full max-w-sm">
    <Notebook>
      <p>Dear diary,</p>
      <p>Today I shipped the design system.</p>
    </Notebook>
  </div>
</PreviewContainer>

## Installation

<InstallTabs slug="notebook" peerDeps={["roughjs", "Font-Caveat"]} />

## Props

<PropsTable
  rows={[
    {
      prop: "id",
      type: "string",
      default: undefined,
      description:
        "Optional stable id used for sketch seeding and accessibility.",
    },
    {
      prop: "lineColor",
      type: "string",
      default: undefined,
      description: "Overrides the notebook line color.",
    },
    {
      prop: "lineSpacing",
      type: "number",
      default: "24",
      description: "Controls the spacing between notebook lines.",
    },
    {
      prop: "marginColor",
      type: "string",
      default: undefined,
      description: "Overrides the notebook margin color.",
    },
    {
      prop: "showMargin",
      type: "boolean",
      default: "true",
      description: "Shows the notebook margin line.",
    },
    {
      prop: "theme",
      type: "CrumbleTheme",
      default: '== "ink"',
      description: "Overrides the global Crumble theme for this instance.",
    },
  ]}
/>

## Without margin

<PreviewContainer
  componentName="notebook"
  code={`<Notebook showMargin={false}>
  <p>No red margin line.</p>
  <p>Just ruled lines.</p>
</Notebook>`}
>
  <div className="w-full max-w-sm">
    <Notebook showMargin={false}>
      <p>No red margin line.</p>
      <p>Just ruled lines.</p>
    </Notebook>
  </div>
</PreviewContainer>

## Custom line spacing

<PreviewContainer
  componentName="notebook"
  code={`<Notebook lineSpacing={36}>
  <p>Wider ruled lines</p>
  <p>More breathing room.</p>
</Notebook>`}
>
  <div className="w-full max-w-sm">
    <Notebook lineSpacing={36}>
      <p>Wider ruled lines.</p>
      <p>More breathing room between lines.</p>
    </Notebook>
  </div>
</PreviewContainer>

## As a card

<PreviewContainer
  componentName="notebook"
  code={`<Notebook className="rounded-xl shadow-sm" lineSpacing={28}>
  <h3 className="font-semibold">Meeting notes</h3>
  <p className="text-muted-foreground">Discussed Q3 roadmap and priorities.</p>
  <p className="text-muted-foreground">Follow up with design team on Monday.</p>
</Notebook>`}
>
  <div className="w-full max-w-sm">
    <Notebook className="rounded-xl shadow-sm" lineSpacing={28}>
      <h3 className="font-semibold">Meeting notes</h3>
      <p className="text-muted-foreground">
        Discussed Q3 roadmap and priorities.
      </p>
      <p className="text-muted-foreground">
        Follow up with design team on Monday.
      </p>
    </Notebook>
  </div>
</PreviewContainer>

## Accessibility

Notebook is a decorative layout primitive. The ruled lines and margin are visual only, so all meaningful content must remain readable in the text children themselves.

## Examples

```tsx
import { Notebook } from "@/components/crumble/ui/notebook";

<Notebook>
  <p>Dear diary,</p>
  <p>Today I shipped the design system.</p>
</Notebook>;
```
