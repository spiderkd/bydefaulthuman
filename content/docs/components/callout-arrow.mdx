---
title: Callout Arrow
description: A hand-drawn curved arrow connecting two DOM elements.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { CalloutDemo } from "@/components/demos/callDemo.tsx";

## Basic Usage

The `CalloutArrow` component creates a dynamic, hand-drawn connection between two elements using React refs. It renders into a fixed SVG overlay, allowing it to span across different layout containers.

<PreviewContainer
  componentName="callout-arrow"
  code={`import { useRef } from "react"
import { CalloutArrow } from "@/components/ui/callout-arrow"

export default function Demo() {
  const fromRef = useRef(null)
  const toRef = useRef(null)

return (

<div className="flex flex-col items-center gap-12">
<div ref={fromRef}>Source</div>
<div ref={toRef}>Target</div>
<CalloutArrow fromRef={fromRef} toRef={toRef} label="Look here!" />
</div>
)
}`}

>

  <CalloutDemo duration={1200} labelTrue />
</PreviewContainer>

## Installation

<InstallTabs slug="callout-arrow" peerDeps={["roughjs"]} />

## Props

<PropsTable
  rows={[
    {
      prop: "className",
      type: "string",
      default: undefined,
      description: "Additional Tailwind classes applied to the component.",
    },
    {
      prop: "color",
      type: "string",
      default: '"currentColor"',
      description: "Overrides the main accent or stroke color.",
    },
    {
      prop: "curvature",
      type: "number",
      default: "80",
      description: "Controls the arrow bend amount.",
    },
    {
      prop: "fromRef",
      type: "RefObject<HTMLElement | null>",
      default: undefined,
      description: "Reference to the starting element.",
      required: true,
    },
    {
      prop: "id",
      type: "string",
      default: undefined,
      description:
        "Optional stable id used for sketch seeding and accessibility.",
    },
    {
      prop: "label",
      type: "ReactNode",
      default: undefined,
      description: "Optional label text shown with the component.",
    },
    {
      prop: "style",
      type: "CSSProperties",
      default: undefined,
      description: "Controls the `style` behavior.",
    },
    {
      prop: "theme",
      type: "CrumbleTheme",
      default: undefined,
      description: "Overrides the global Crumble theme for this instance.",
    },
    {
      prop: "toRef",
      type: "RefObject<HTMLElement | null>",
      default: undefined,
      description: "Reference to the target element.",
      required: true,
    },
    {
      prop: "withHead",
      type: "boolean",
      default: "true",
      description: "Shows an arrow head at the end of the callout.",
    },
  ]}
/>

## Customization

### Curvature

Adjust the `curvature` prop to change the arc. Positive values bow one way, negative values the other, and `0` results in a straight line.

<PreviewContainer
  componentName="callout-arrow"
  code={`<CalloutArrow fromRef={f} toRef={t} curvature={-100} label="Inverse" />`}
>
  <CalloutDemo curvature={-80} label="Inverse Curve" labelTrue={false} />
</PreviewContainer>

### Color

The arrow and label inherit the current text color by default, but you can pass any valid CSS color string to the `color` prop.

<PreviewContainer
  componentName="callout-arrow"
  code={`<CalloutArrow color="oklch(0.6 0.22 25)" label="Vibrant" />`}
>
  <CalloutDemo
    color="oklch(0.6 0.22 25)"
    label="Custom Color"
    labelTrue={false}
  />
</PreviewContainer>

## Accessibility

CalloutArrow is decorative and renders `aria-hidden`. Any relationship the arrow implies between two elements should also be explained in nearby text.

## Examples

```tsx
import { useRef } from "react";
import { CalloutArrow } from "@/components/ui/callout-arrow";

export default function Demo() {
  const fromRef = useRef(null);
  const toRef = useRef(null);

  return (
    <div className="flex flex-col items-center gap-12">
      <div ref={fromRef}>Source</div>
      <div ref={toRef}>Target</div>
      <CalloutArrow fromRef={fromRef} toRef={toRef} label="Look here!" />
    </div>
  );
}
```
