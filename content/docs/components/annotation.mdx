---
title: Annotation
description: Wrap any element with a hand-drawn annotation that draws itself on mount.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { Annotation } from "@/registry/new-york/ui/annotation";

---

## Basic

<PreviewContainer
  componentName="annotation"
  code={`import { Annotation } from '@/components/crumble/ui/annotation'

<Annotation type="box">
  <span className="text-base font-medium">Annotated text</span>
</Annotation>`}
>
  <Annotation type="box">
    <span className="text-base font-medium">Annotated text</span>
  </Annotation>
</PreviewContainer>

## Installation

<InstallTabs slug="annotation" peerDeps={["rough-notation","roughjs"]} />

## Props

<PropsTable
  rows={[
    {
      prop: "animate",
      type: "boolean",
      default: "true",
      description: "Controls the `animate` behavior.",
    },
    {
      prop: "animationDelay",
      type: "number",
      default: "0",
      description: "Animation delay in milliseconds.",
    },
    {
      prop: "animationDuration",
      type: "number",
      default: undefined,
      description: "Animation duration in milliseconds.",
    },
    {
      prop: "brackets",
      type: "BracketSide | BracketSide[]",
      default: undefined,
      description: "Bracket sides drawn when using bracket annotations.",
    },
    {
      prop: "color",
      type: "string",
      default: '"currentColor"',
      description: "Overrides the main accent or stroke color.",
    },
    {
      prop: "iterations",
      type: "number",
      default: undefined,
      description: "Number of sketch passes used by the annotation.",
    },
    {
      prop: "label",
      type: "ReactNode",
      default: undefined,
      description: "Optional label text shown with the component.",
    },
    {
      prop: "labelSide",
      type: "AnnotationSide",
      default: '"top"',
      description: "Controls the `labelSide` behavior.",
    },
    {
      prop: "multiline",
      type: "boolean",
      default: "false",
      description: "Handles wrapped text as multiple annotation lines.",
    },
    {
      prop: "padding",
      type: "number | [number, number] | [number, number, number, number]",
      default: "5",
      description: "Adds padding around the component content.",
    },
    {
      prop: "show",
      type: "boolean",
      default: undefined,
      description: "Controls the `show` behavior.",
    },
    {
      prop: "strokeWidth",
      type: "number",
      default: undefined,
      description: "Controls the `strokeWidth` behavior.",
    },
    {
      prop: "theme",
      type: "CrumbleTheme",
      default: undefined,
      description: "Overrides the global Crumble theme for this instance.",
    },
    {
      prop: "trigger",
      type: "AnnotationTrigger",
      default: '"inView" | "hover" | "mount".',
      description: "Controls when the annotation is shown.",
    },
    {
      prop: "type",
      type: "AnnotationType",
      default: undefined,
      description: "Controls the visual variant or drawing style.",
    },
  ]}
/>

---

## Types

<PreviewContainer
  componentName="annotation-types"
  code={`<Annotation type="box">Box</Annotation>
<Annotation type="circle">Circle</Annotation>
<Annotation type="underline">Underline</Annotation>
<Annotation type="highlight">Highlight</Annotation>
<Annotation type="strike-through">Strike</Annotation>
<Annotation type="crossed-off">Crossed</Annotation>
<Annotation type="bracket">Bracket</Annotation>`}
>
  <div className="flex flex-wrap gap-6 py-4">
    <Annotation type="box">
      <span>Box</span>
    </Annotation>
    <Annotation type="circle">
      <span>Circle</span>
    </Annotation>
    <Annotation type="underline">
      <span>Underline</span>
    </Annotation>
    <Annotation type="highlight" color="oklch(0.6 0.18 260)">
      <span>Highlight</span>
    </Annotation>
    <Annotation type="strike-through">
      <span>Strike</span>
    </Annotation>
    <Annotation type="crossed-off">
      <span>Crossed</span>
    </Annotation>
    <Annotation type="bracket">
      <span>Bracket</span>
    </Annotation>
  </div>
</PreviewContainer>

---

## Labels

<PreviewContainer
  componentName="annotation-label"
  code={`<Annotation
  type="box"
  label="Important"
  labelSide="top"
  color="oklch(0.6 0.18 260)"
>
  Feature
</Annotation>`}
>
  <div className="py-10">
    <Annotation
      type="box"
      label="Important"
      labelSide="top"
      color="oklch(0.6 0.18 260)"
    >
      <span className="text-base font-semibold">Key feature</span>
    </Annotation>
  </div>
</PreviewContainer>

---

## Trigger: Hover

<PreviewContainer
  componentName="annotation-hover"
  code={`<Annotation type="circle" trigger="hover">
  Hover me
</Annotation>`}
>
  <Annotation type="circle" trigger="hover">
    <span>Hover me</span>
  </Annotation>
</PreviewContainer>

---

## Trigger: In View

<PreviewContainer
  componentName="annotation-inview"
  code={`<Annotation type="highlight" trigger="inView">
  Scroll into view
</Annotation>`}
>
  <div className="h-[200px] flex items-center justify-center">
    <Annotation type="highlight" trigger="inView" color="oklch(0.6 0.18 260)">
      <span>Scroll into view</span>
    </Annotation>
  </div>
</PreviewContainer>

---

## Animation Control

<PreviewContainer
  componentName="annotation-animation"
  code={`<Annotation
  type="underline"
  animationDuration={1200}
  animationDelay={300}
>
  Slow animation
</Annotation>`}
>
  <Annotation type="underline" animationDuration={1200} animationDelay={300}>
    <span>Slow animation</span>
  </Annotation>
</PreviewContainer>

---

## Disable Animation

<PreviewContainer
  componentName="annotation-no-animation"
  code={`<Annotation type="circle" animate={false}>
  Static annotation
</Annotation>`}
>
  <Annotation type="circle" animate={false}>
    <span>Static annotation</span>
  </Annotation>
</PreviewContainer>

---

## Multiline

<PreviewContainer
  componentName="annotation-multiline"
  code={`<Annotation type="underline" multiline>
  This is a long text that wraps across multiple lines
</Annotation>`}
>
  <div className="max-w-xs">
    <Annotation type="bracket" multiline>
      This is a long text that wraps across multiple lines and still gets
      annotated correctly.
    </Annotation>
  </div>
</PreviewContainer>

---

## Custom Color

<PreviewContainer
  componentName="annotation-color"
  code={`<Annotation type="box" color="oklch(0.6 0.22 25)">
  Coral box
</Annotation>`}
>
  <Annotation type="box" color="oklch(0.6 0.22 25)">
    <span>Coral box</span>
  </Annotation>
</PreviewContainer>

---

## In a Paragraph

<PreviewContainer
  componentName="annotation-paragraph"
  code={`<p>
  The most <Annotation type="underline">important thing</Annotation> is to ship.
</p>`}
>
  <p className="text-base leading-relaxed max-w-lg">
    The most{" "}
    <Annotation type="underline" color="oklch(0.65 0.18 55)">
      important thing
    </Annotation>{" "}
    is to ship.
  </p>
</PreviewContainer>

---

## Accessibility

Annotation adds emphasis around existing text but does not change the underlying semantics. Do not use the annotation stroke as the only way to communicate importance or status.

## Examples

```tsx
import { Annotation } from "@/components/crumble/ui/annotation";

<Annotation type="box">
  <span className="text-base font-medium">Annotated text</span>
</Annotation>;
```
