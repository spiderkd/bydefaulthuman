---
title: Tooltip
description: A hand-drawn callout that appears on hover with a rough polygon arrow.
---

import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";
import { InstallTabs } from "@/components/mdx/InstallTabs";
import { Tooltip } from "@/registry/new-york/ui/tooltip";
import { Button } from "@/registry/new-york/ui/button";

<PreviewContainer
  componentName="tooltip"
  code={`import { Tooltip } from '@/components/crumble/ui/tooltip'

<Tooltip content="Save your work">
  <Button>Save</Button>
</Tooltip>`}
>
  <Tooltip content="Save your work">
    <Button>Save</Button>
  </Tooltip>
</PreviewContainer>

## Installation

<InstallTabs slug="tooltip" />

## Props

<PropsTable rows={[
  { prop: 'children', type: "ReactNode", default: undefined, description: "Content rendered inside the component.", required: true },
  { prop: 'className', type: "string", default: "\"relative inline-flex\"", description: "Additional Tailwind classes applied to the component." },
  { prop: 'content', type: "ReactNode", default: undefined, description: "Content rendered inside the overlay.", required: true },
  { prop: 'delayMs', type: "number", default: "400", description: "Delay before the tooltip opens, in milliseconds." },
  { prop: 'id', type: "string", default: undefined, description: "Optional stable id used for sketch seeding and accessibility." },
  { prop: 'side', type: "TooltipSide", default: "\"top\"", description: "Preferred side or placement." },
  { prop: 'theme', type: "CrumbleTheme", default: undefined, description: "Overrides the global Crumble theme for this instance." },
]} />

## Sides

<PreviewContainer
  componentName="tooltip"
  code={`<Tooltip content="On top" side="top"><Button size="sm">Top</Button></Tooltip>
<Tooltip content="On the right" side="right"><Button size="sm">Right</Button></Tooltip>
<Tooltip content="At the bottom" side="bottom"><Button size="sm">Bottom</Button></Tooltip>
<Tooltip content="On the left" side="left"><Button size="sm">Left</Button></Tooltip>`}
>
  <div className="flex flex-wrap gap-3">
    <Tooltip content="On top" side="top">
      <Button size="sm">Top</Button>
    </Tooltip>
    <Tooltip content="On the right" side="right">
      <Button size="sm">Right</Button>
    </Tooltip>
    <Tooltip content="At the bottom" side="bottom">
      <Button size="sm">Bottom</Button>
    </Tooltip>
    <Tooltip content="On the left" side="left">
      <Button size="sm">Left</Button>
    </Tooltip>
  </div>
</PreviewContainer>

## Custom delay

<PreviewContainer
  componentName="tooltip"
  code={`<Tooltip content="Appears instantly" delayMs={0}>
  <Button size="sm">No delay</Button>
</Tooltip>`}
>
  <Tooltip content="Appears instantly" delayMs={0}>
    <Button size="sm">No delay</Button>
  </Tooltip>
</PreviewContainer>

## Themes

<PreviewContainer
  componentName="tooltip"
  code={`<Tooltip content="Pencil tooltip" theme="pencil"><Button size="sm" theme="pencil">Pencil</Button></Tooltip>
<Tooltip content="Ink tooltip" theme="ink"><Button size="sm" theme="ink">Ink</Button></Tooltip>
<Tooltip content="Crayon tooltip" theme="crayon"><Button size="sm" theme="crayon">Crayon</Button></Tooltip>`}
>
  <div className="flex flex-wrap gap-3">
    <Tooltip content="Pencil tooltip" theme="pencil">
      <Button size="sm" theme="pencil">
        Pencil
      </Button>
    </Tooltip>
    <Tooltip content="Ink tooltip" theme="ink">
      <Button size="sm" theme="ink">
        Ink
      </Button>
    </Tooltip>
    <Tooltip content="Crayon tooltip" theme="crayon">
      <Button size="sm" theme="crayon">
        Crayon
      </Button>
    </Tooltip>
  </div>
</PreviewContainer>

## Accessibility

Tooltip content is supplemental, not primary. Do not put critical instructions only inside the tooltip, because hover-only disclosure is easy to miss on touch devices and with keyboard navigation.

## Examples

```tsx
import { Tooltip } from '@/components/crumble/ui/tooltip'

<Tooltip content="Save your work">
  <Button>Save</Button>
</Tooltip>
```
