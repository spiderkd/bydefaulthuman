---
title: Dashboard Block
description: A stats overview block combining StatCard, Sparkline, and BarChart in a hand-drawn grid.
---

import { DashboardBlockDemo } from "@/components/block/demos/DashboardBlockDemo";
import { PreviewContainer } from "@/components/preview/PreviewContainer";
import { PropsTable } from "@/components/mdx/PropsTable";

<PreviewContainer
  componentName="stat-card"
  code={`import { DashboardBlock } from '@/components/block/DashboardBlock'

<DashboardBlock />`}
>
  <DashboardBlockDemo />
</PreviewContainer>

## Installation

```bash
npx shadcn add https://crumble.dev/r/stat-card.json
npx shadcn add https://crumble.dev/r/sparkline.json
npx shadcn add https://crumble.dev/r/bar-chart.json
npx shadcn add https://crumble.dev/r/card.json
```

Then copy `components/block/DashboardBlock.tsx` into your project.

## Props

<PropsTable rows={[
  { prop: 'barColor', type: "string", default: undefined, description: "Default chart color." },
  { prop: 'barData', type: "BarPoint[]", default: undefined, description: "Bar chart data." },
  { prop: 'barLabel', type: "string", default: undefined, description: "Label shown above the bar chart." },
  { prop: 'sparkLabel', type: "string", default: undefined, description: "Label shown above the sparkline group." },
  { prop: 'sparkRows', type: "SparkRow[]", default: undefined, description: "Sparkline row data." },
  { prop: 'stats', type: "DashboardStat[]", default: undefined, description: "Stat card data for the block." },
]} />


## Custom stats

```tsx
<DashboardBlock
  stats={[
    { label: "MRR", value: "$9,200", trend: "up", trendLabel: "+18%" },
    { label: "Trials", value: "142", trend: "up", trendLabel: "+5%" },
    { label: "Churned", value: "8", trend: "down", trendLabel: "-3" },
    { label: "NPS", value: "72", trend: "flat", trendLabel: "stable" },
  ]}
/>
```

## Accessibility

This block is composed from accessible Crumble components. Preserve visible labels, button text, and supporting copy when adapting it so the layout remains understandable without the visual styling.

## Examples

```tsx
import { DashboardBlock } from '@/components/block/DashboardBlock'

<DashboardBlock />
```
